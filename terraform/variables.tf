# The variables in this file are now defined in `config/main.yml` and `config/secrets.yml`.
# This file is kept for reference, but it is no longer used.
#
# You can generate a new terraform.tfvars file by running the following command:
#
# yq eval '... a yq query to transform the yaml to tfvars ...' ../config/main.yml ../config/secrets.yml > terraform.tfvars
